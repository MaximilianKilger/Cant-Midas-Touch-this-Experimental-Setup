from controls.opportunistic_control import OpportunisticControl

# Base Class for Valuator-type controls.
# Valuators output a continuous value (normalized between 0 and 1) depending on the tangible's state.
class Valuator(OpportunisticControl):

    def __init__():
        return
    
    def getValue() -> float:
        return